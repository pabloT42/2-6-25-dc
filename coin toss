#include<iostream>
using namespace std;
void CoinToss();

int main() {

	CoinToss();
}

void CoinToss() {

	srand(time(NULL));
	int num = rand() % 100 + 1;
 
	if (num < 50) {
		cout << "Tails" << endl;
	}
 
	else {
		cout << "Heads" << endl;
	}
 
}
